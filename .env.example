# Django
DJANGO_SECRET_KEY=replace-with-a-long-random-secret

# MySQL
DB_NAME=edu_chatbot
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Email (leave blank when email is not configured)
EMAIL_USE_TLS=True
EMAIL_HOST=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_PORT=587
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
DEFAULT_FROM_EMAIL=
